[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.poetry.dependencies]
python = "^3.9"
tensorflow = "^2.18.0"
pytest = "^8.3.4"
flake8 = "^7.1.1"
scikit-learn = "^1.6.1"
pandas = "^2.2.3"
